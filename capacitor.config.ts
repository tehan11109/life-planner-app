import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ta.lifeplanner',
  appName: 'Life & Finance Planner',
  webDir: 'dist'
};

export default config;
